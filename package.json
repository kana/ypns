{
  "name": "ypns",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "forever": "while true; do { echo; date; npm start; } 2>&1 | tee -a ypns.log; sleep 300; done",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "cheerio": "^0.20.0",
    "dateformat": "^1.0.12",
    "request": "^2.74.0",
    "request-promise": "^4.0.2",
    "slack": "^7.5.7",
    "tough-cookie-filestore": "git+https://github.com/kana/tough-cookie-filestore.git#sync-write"
  }
}
